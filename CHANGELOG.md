# Changelog

## [1.2.0] — 2026-03-11

### Добавлено
- 🎵 `--sound-input` — отдельный звук когда Claude ждёт твоего ввода (`Ping` на Mac)
- 🎵 `--sound-all` — разные звуки на все события: завершение / ввод / разрешение
- 🔔 `--notify-input` — пуш уведомление когда Claude ждёт ввода
- 🔔 `--notify-all` — уведомления на все события
- 🎨 `--sound-done "/path"` и `--sound-input "/path"` — кастомные звуки
- Хуки на `Notification` и `PermissionRequest` события Claude Code
- Поддержка Windows (PowerShell beep + MessageBox)
- Mac: Glass (завершение) / Ping (ввод) / Sosumi (разрешение)

---


## [1.1.0] — 2026-03-11

### Добавлено
- 🔊 `--sound` — звук когда Claude Code заканчивает отвечать (macOS + Linux)
- 🔔 `--notify` — системное уведомление по завершению
- 🎲 `random` — случайный пак фраз при каждом запуске
- ✏️ `--add "фраза"` — добавить свою фразу к текущим
- 🚀 `--all-features` — установить всё одной командой
- 🔄 `--reset` — сбросить к стандартным настройкам Claude Code

---

## [1.0.0] — 2026-03-11

### Первый релиз
- 😤 Пак `ru-vibe` — русский вайб (28 фраз)
- 🏢 Пак `corporate` — корпоратный ад (28 фраз)
- 🌀 Пак `existential` — экзистенциальный кризис (28 фраз)
- 🚀 Пак `startup` — стартап-режим (28 фраз)
- `all` — установить все 4 пака сразу (112 фраз)
- Не ломает другие настройки `~/.claude/settings.json`
